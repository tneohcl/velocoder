**Save to** (bottom of the queue) is the folder every converted video is written to. Click **Change...** to pick a different one, or type a path directly into the field. **Open** reveals that folder in your file manager.

Converted files never overwrite or replace the originals -- they're written alongside them in this separate folder.
