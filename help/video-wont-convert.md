A row showing **Failed** means that video's conversion hit a real error -- hover the row (or check the Conversion Log) for the reason. Common causes: the source file is corrupted or in a format VeloCoder can't read, or there isn't enough free space in the Save-to folder.

Fixing the underlying issue and pressing **Convert** again will retry it along with anything else still unfinished in the queue.
