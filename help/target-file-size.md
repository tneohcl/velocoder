Switch Mode to **File Size** to aim for a specific output size (in MB) instead of a quality level. VeloCoder works out the bitrate needed to hit that target from the video's own length -- a longer video needs a lower bitrate to land on the same size.

Very short target sizes for a long video can look noticeably worse, since there's only so much data left to spend on each second.
